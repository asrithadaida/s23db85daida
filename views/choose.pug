block content
  h1= title
  - var image_names = ['vehicle1.jpg', 'vehicle2.jpg', 'vehicle3.jpg', 'vehicle4.jpg', 'vehicle5.jpg']

  - for(let i = 0; i<3; i++)
    - let index = Math.floor(Math.random()*5)
    img(src=`/images/${ image_names[index]}`, alt=`${ image_names[index]}`, height="300", width="300")